# 📝 Registration Form – Functional Component

This project is a **React Registration Form** built using a **Functional Component**.  
It demonstrates how to handle forms in React using the `useState` hook.
